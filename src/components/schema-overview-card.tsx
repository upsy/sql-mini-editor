import { Card, CardContent, CardFooter, CardHeader, CardTitle } from "@/components/ui/card";


export function SchemaOverviewCard(){
    return   (
    <Card className="w-full">
      <CardHeader>
        <CardTitle>Schema Overview</CardTitle>
      </CardHeader>
      <CardContent>
        
      </CardContent>
      <CardFooter>
      </CardFooter>
    </Card>)
}